// wasm loader - loads the wasm file generated from Rust.

// NOTE: This is a work in progress on bindings.
// Will bind a regular js file first, then build out bindings.
// @module("WebAssembly") external instantiateStreaming: string => 'wasm = "instantiateStreaming"

// 1. Fetch the wasm data in a promise
// 2. Convert to bytes??? IDFK

